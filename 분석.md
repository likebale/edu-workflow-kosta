# TaskFlow 코드베이스 분석 보고서

**분석 일시**: 2026-07-13  
**분석 도구**: Claude Code analyze-codebase 스킬 (6단계)  
**프로젝트**: TaskFlow (KOSTA 바이브코딩 실습용 관통 프로젝트)  
**저장소 루트**: `/Users/kimkunho/VSCodeProjects/taskflow-starter`

---

## 목차

1. [(1) 의존성과 스택 확인](#1-의존성과-스택-확인)
2. [(2) 폴더 구조와 계층 파악](#2-폴더-구조와-계층-파악)
3. [(3) 핵심 데이터 모델 지목](#3-핵심-데이터-모델-지목)
4. [(4) 주요 엔드포인트와 상태 코드 정리](#4-주요-엔드포인트와-상태-코드-정리)
5. [(5) 한 요청의 흐름 끝까지 추적](#5-한-요청의-흐름-끝까지-추적)
6. [(6) 숨은 규약과 약점 발견](#6-숨은-규약과-약점-발견)
7. [최종 요약](#최종-요약)

---

## (1) 의존성과 스택 확인

### 의존도 흐름

```
Routes (taskRoutes.js)
    ↓
Controllers (taskController.js)
    ↓
Services (taskService.js)
    ↓
Database (database.js)
    ↓
SQLite
```

### 검증 결과

| 항목 | 검증 | 결과 |
|------|------|------|
| 단방향 의존도 | ✅ | Routes → Controllers → Services → DB (완벽함) |
| 역방향 의존 | ✅ | 없음 |
| 계층 건너뛰기 | ✅ | 없음 |
| 순환 의존성 | ✅ | 없음 |

### 결론
✅ **아키텍처적으로 정상** — 계층 분리가 명확하고 의존도가 일방향

---

## (2) 폴더 구조와 계층 파악

### 전체 디렉터리 구조

```
taskflow-starter/
├── .claude/
│   ├── settings.json
│   └── skills/
│       └── analyze-codebase/
│           └── SKILL.md
├── controllers/
│   └── taskController.js
├── db/
│   └── database.js
├── routes/
│   └── taskRoutes.js
├── services/
│   └── taskService.js
├── tests/
│   ├── taskController.test.js
│   └── taskService.test.js
├── views/
│   └── index.ejs
├── .env.example
├── .gitignore
├── app.js
├── CLAUDE.md
├── README.md
├── package.json
└── package-lock.json
```

### 파일 통계

| 확장자 | 개수 | 설명 |
|--------|------|------|
| `.js` | 7개 | 소스 코드 |
| `.json` | 3개 | 설정 파일 |
| `.md` | 2개 | 문서 |
| `.ejs` | 1개 | 템플릿 |

### 계층별 책임

| 계층 | 파일 | 책임 (있음) | 책임 (없음) |
|------|------|-----------|-----------|
| **Routes** | `taskRoutes.js` | 6개 엔드포인트 정의 | 검증, SQL, 응답 변환 |
| **Controllers** | `taskController.js` | 입력 검증, DTO 변환, 상태 코드 | SQL, 비즈니스 로직 |
| **Services** | `taskService.js` | CRUD, SQL 실행 | 검증, 응답 변환 |
| **Database** | `database.js` | 연결, 스키마 초기화 | 비즈니스 로직 |
| **Views** | `index.ejs` | HTML 렌더링, 데이터 표시 | 데이터 가공 |

---

## (3) 핵심 데이터 모델 지목

### 핵심 엔티티: task

| 계층 | 사용 방식 |
|------|---------|
| Routes | `/api/tasks`, `:id` 파라미터 |
| Controllers | 함수 파라미터, 변수명 |
| Services | 함수명 (getAll, getById, create, update, remove), 테이블명 |
| Database | 테이블 `tasks` |
| Views | 배열 `tasks`, 요소 `t` |

### 주요 필드

| 필드 | 타입 | 검증 | 기본값 | 비고 |
|------|------|------|--------|------|
| **id** | INTEGER | PK, AUTO INCREMENT | — | DB 자동 생성 |
| **title** | TEXT | ✅ 필수, 비공백, 문자열 | — | **유일하게 검증됨** |
| **description** | TEXT | ❌ | NULL | 선택 필드 |
| **status** | TEXT | ❌ | 'todo' | ⚠️ enum 검증 없음 |
| **assignee** | TEXT | ❌ | NULL | 선택 필드 |
| **created_at** | TEXT | — | datetime('now') | 자동 생성, 수정 불가 |

### 핵심 함수

**검증**
- `validateTaskInput(body)` (controllers/taskController.js:5-10)
  - title만 검증 (필수, 비공백, 문자열)
  - description, assignee, status는 검증 안 함

**변환**
- `toTaskDTO(task)` (controllers/taskController.js:13-23)
  - DB 행 → JSON 응답 변수 포맷
  - API 응답에만 사용 (뷰는 원본 데이터)

**CRUD**
- `taskService.create()` (services/taskService.js:12-17) — INSERT + 재조회
- `taskService.update()` (services/taskService.js:20-31) — UPDATE (COALESCE로 부분 업데이트)
- `taskService.getAll()` (services/taskService.js:4-6) — SELECT 전체
- `taskService.getById()` (services/taskService.js:8-10) — SELECT 단일
- `taskService.remove()` (services/taskService.js:34-37) — DELETE

---

## (4) 주요 엔드포인트와 상태 코드 정리

### REST API 스펙

| 메서드 | 경로 | 기능 | 성공 | 실패 | 응답 형식 |
|--------|------|------|------|------|----------|
| **GET** | `/` | 태스크 목록 (HTML 뷰) | 200 | 500 | HTML |
| **GET** | `/api/tasks` | 태스크 목록 (JSON) | 200 | 500 | JSON 배열 |
| **POST** | `/api/tasks` | 태스크 생성 | **201** | 400/500 | JSON (task) |
| **PUT** | `/api/tasks/:id` | 태스크 부분 업데이트 | 200 | 404/500 | JSON (task) |
| **DELETE** | `/api/tasks/:id` | 태스크 삭제 | **204** | 404/500 | (없음) |

### 상태 코드 규약

- **200**: 조회 성공, 부분 업데이트 성공
- **201**: 생성 성공
- **204**: 삭제 성공 (응답 본문 없음)
- **400**: 검증 실패 (title 누락 등)
- **404**: 리소스 없음 (해당 ID 없음)
- **500**: 서버 에러 (미처리 예외)

### 응답 형식

**성공 응답 (201/200)**
```json
{
  "id": 1,
  "title": "새 태스크",
  "description": null,
  "status": "todo",
  "assignee": "김개발",
  "created_at": "2026-07-13 HH:MM:SS"
}
```

**실패 응답 (400)**
```json
{ "error": "title 은 필수입니다" }
```

**삭제 성공 (204)**
```
(응답 본문 없음)
```

---

## (5) 한 요청의 흐름 끝까지 추적

### POST /api/tasks 요청 경로

#### 예시 입력
```json
POST /api/tasks
Content-Type: application/json

{
  "title": "새 태스크",
  "assignee": "김개발"
}
```

### 단계별 흐름

**[1] Express 진입점**
- 파일: `app.js:17`
- 내용: `app.use('/', taskRoutes);`

**[2] 라우트 마운트**
- 파일: `routes/taskRoutes.js:8`
- 내용: `router.post('/api/tasks', taskController.create);`

**[3] 컨트롤러 - 검증**
- 파일: `controllers/taskController.js:35-42`
- 함수: `create(req, res)`
- 입력: `req.body = { title: "새 태스크", assignee: "김개발" }`

검증 호출:
- 파일: `controllers/taskController.js:5-10`
- 함수: `validateTaskInput(body)`
- 처리: title 필수, 비공백, 문자열 확인
- 반환: `{ valid: true }`

**[4] 컨트롤러 - 서비스 호출**
- 파일: `controllers/taskController.js:40`
- 내용: `taskService.create(req.body)`

**[5] 서비스 - INSERT 실행**
- 파일: `services/taskService.js:12-17`
- 함수: `create({ title, description, assignee })`
- SQL: `INSERT INTO tasks (title, description, assignee) VALUES (?, ?, ?)`
- 파라미터: `("새 태스크", null, "김개발")`
- DB 결과:
  ```sql
  -- status = 'todo' (DEFAULT)
  -- created_at = datetime('now') (DEFAULT)
  ```
- 반환: `info.lastInsertRowid = 1`

**[6] 서비스 - 재조회**
- 파일: `services/taskService.js:16`
- 함수: `getById(1)`
- SQL: `SELECT * FROM tasks WHERE id = ?`
- 반환: `{ id: 1, title: "새 태스크", description: null, status: "todo", assignee: "김개발", created_at: "..." }`

**[7] 컨트롤러 - DTO 변환**
- 파일: `controllers/taskController.js:13-23`
- 함수: `toTaskDTO(task)`
- 반환: (동일한 구조의 JSON 객체)

**[8] HTTP 응답**
- 상태 코드: `201 Created`
- Content-Type: `application/json`
- 응답 본문: task 객체

### 흐름도

```
HTTP Request (POST /api/tasks)
    ↓
taskRoutes.js:8 (라우트 매칭)
    ↓
taskController.create()
    ├─ validateTaskInput() → 통과
    └─ taskService.create()
        ├─ db.prepare().run() (INSERT)
        └─ getById() (SELECT)
            ↓
            toTaskDTO() (변환)
            ↓
HTTP 201 Created (JSON 응답)
```

### 에러 경로 (title 없을 때)

```
validateTaskInput(body) → { valid: false, message: "title 은 필수입니다" }
    ↓
res.status(400).json({ error: "..." })
    ↓
HTTP 400 Bad Request (서비스/DB 도달 안 함)
```

---

## (6) 숨은 규약과 약점 발견

### 발견된 약점 (우선순위순)

| 우선순위 | 파일:라인 | 문제점 | 심각도 |
|---------|----------|--------|--------|
| **1** | `services/taskService.js:20` | **status enum 검증 부재** — `update()` 시 status를 검증 없이 DB에 저장. 임의의 값 가능 | 🔴 |
| **2** | `controllers/taskController.js:35-51` | **create/update 검증 비대칭** — create는 title 검증, update는 검증 없음. 빈 문자열도 가능 | 🔴 |
| **3** | `controllers/taskController.js:25-28` | **listView에서 DTO 미변환** — 뷰는 원본 DB 데이터, API는 toTaskDTO(). 데이터 처리 비대칭 | 🟠 |
| **4** | `routes/taskRoutes.js:9` | **PUT 폼 미지원** — HTML `<form>` 태그는 GET/POST만 지원. 뷰에서 상태 변경 불가 | 🟠 |
| **5** | `controllers/taskController.js:44-51` | **update 검증 부재** — 빈 객체 {} 도 HTTP 200으로 허용. 검증 전무 | 🟡 |
| **6** | `controllers/taskController.js:53-60` | **응답 일관성 부족** — DELETE는 204 + 본문 없음, CREATE는 201 + 본문, UPDATE는 200 + 본문 | 🟡 |
| **7** | `services/taskService.js:12-17` | **description/assignee 검증 부재** — 타입 검증 없음. 객체, 배열, 매우 긴 문자열도 저장 가능 | 🟡 |

### 숨은 규약

**검증 위치**
- Controllers에서만 검증 (validateTaskInput)
- Services는 입력을 무조건 신뢰
- DB 레벨에는 제약 거의 없음

**DTO 변환**
- API 응답만 toTaskDTO() 사용 (listApi, create, update)
- 뷰 렌더링은 원본 데이터 직접 사용 (listView)

**자동 값 설정**
- status = 'todo' (INSERT 시에만, UPDATE 시는 COALESCE 사용)
- created_at = datetime('now') (INSERT 시만)

**부분 업데이트**
- PUT이 PATCH처럼 동작 (COALESCE로 nullable 처리)
- REST 관례상 비표준 (PUT은 전체 교체를 의미하지만 여기선 부분 변경)

---

## 최종 요약

### CLAUDE.md 추가 권장 내용

#### 아키텍처
```markdown
4계층 구조 (Routes → Controllers → Services → Database)
- 단방향 의존도 (역방향/건너뛰기 없음)
- 각 계층은 바로 아래 계층만 호출
- HTTP 요청 흐름: 라우트 → 검증 → 비즈니스 로직 → SQL → DB
```

#### 데이터 모델
```markdown
엔티티: task (테이블: tasks, 6개 필드)
- title: 필수, 빈 문자열 불허 (검증됨)
- status: 기본값 'todo' (검증 없음 — 약점)
- 자동값: id (auto-increment), created_at (now())

핵심 함수:
- validateTaskInput(): title만 검증
- toTaskDTO(): API 응답 변환
- taskService.create/update/remove(): CRUD
```

#### REST API
```markdown
6개 엔드포인트, 상태 코드 규약:
- 201: 생성, 200: 조회/수정, 204: 삭제
- 에러: 400 (검증), 404 (없음), 500 (서버)
```

### 아키텍처 평가

| 항목 | 결과 | 상태 |
|------|------|------|
| 계층 분리 | 명확 (4계층) | ✅ |
| 의존도 방향 | 단방향 | ✅ |
| SQL 중앙화 | Services만 | ✅ |
| 검증 | title만 | ⚠️ |
| 상태 코드 | 규약 준수 | ✅ |
| 데이터 비대칭 | 뷰/API 다름 | ⚠️ |

### 우선 수정 대상

1. **Status enum 검증** (우선순위 1)
2. **create/update 검증 비대칭** (우선순위 2)
3. **뷰/API 데이터 처리 통일** (우선순위 3)

---

**분석 도구**: Claude Code analyze-codebase 스킬 v0.2.0  
**분석 완료 날짜**: 2026-07-13
