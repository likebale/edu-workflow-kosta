# TaskFlow 6단계 분석 요약

## (1) 의존성과 스택 확인

**구조**: Routes → Controllers → Services → Database

**검증**:
- ✅ 단방향 의존도 (역방향 없음)
- ✅ 계층 건너뛰기 없음
- ✅ 순환 의존성 없음

**결론**: 아키텍처적으로 정상, 계층 분리가 명확함

---

## (2) 폴더 구조와 계층 파악

### 파일 구조
```
controllers/taskController.js     — 검증, DTO 변환, 응답 처리
routes/taskRoutes.js             — 6개 엔드포인트 정의
services/taskService.js          — 비즈니스 로직, SQL 실행
db/database.js                   — SQLite 초기화, 스키마
views/index.ejs                  — HTML 렌더링
tests/                           — 단위/통합 테스트
```

### 계층별 책임

| 계층 | 책임 있음 | 책임 없음 |
|------|---------|---------|
| **Routes** | 엔드포인트 매핑 | 검증, SQL, 응답 변환 |
| **Controllers** | 검증, DTO, 상태 코드 | SQL, 비즈니스 로직 |
| **Services** | CRUD, SQL | 검증, 응답 변환 |
| **Database** | 연결, 스키마 | 비즈니스 로직 |

---

## (3) 핵심 데이터 모델 지목

### 주요 필드

| 필드 | 타입 | 검증 | 기본값 | 비고 |
|------|------|------|--------|------|
| **id** | INTEGER | PK | AUTO | 자동 생성 |
| **title** | TEXT | ✅ 필수 | — | 유일하게 검증됨 |
| **description** | TEXT | ❌ | NULL | 선택 |
| **status** | TEXT | ❌ | 'todo' | ⚠️ enum 검증 없음 |
| **assignee** | TEXT | ❌ | NULL | 선택 |
| **created_at** | TEXT | — | now() | 자동 생성 |

### 핵심 함수

- **validateTaskInput()**: title만 검증 (필수, 비공백, 문자열)
- **toTaskDTO()**: DB 행 → JSON 응답 변환
- **taskService.create/update/remove()**: SQL 실행

---

## (4) 주요 엔드포인트와 상태 코드 정리

| 메서드 | 경로 | 기능 | 상태 | 응답 |
|--------|------|------|------|------|
| **GET** | `/` | 뷰 렌더링 | 200 | HTML |
| **GET** | `/api/tasks` | 목록 조회 | 200 | JSON 배열 |
| **POST** | `/api/tasks` | 생성 | **201** | JSON task |
| **PUT** | `/api/tasks/:id` | 부분 업데이트 | 200 | JSON task |
| **DELETE** | `/api/tasks/:id` | 삭제 | **204** | (없음) |

### 에러 코드
- **400**: 검증 실패 (title 누락)
- **404**: 리소스 없음 (해당 ID 없음)
- **500**: 서버 에러

---

## (5) 한 요청의 흐름 끝까지 추적

### POST /api/tasks 흐름

```
1. HTTP 요청 수신
   ↓
2. routes/taskRoutes.js:8 — 라우트 매칭
   ↓
3. controllers/taskController.js:35 — create() 호출
   ├─ validateTaskInput(req.body) — title 검증
   └─ 검증 통과 → 다음 단계
   ↓
4. services/taskService.js:12 — create() 호출
   ├─ INSERT INTO tasks (title, description, assignee)
   ├─ status, created_at은 DB DEFAULT 적용
   └─ info.lastInsertRowid 획득
   ↓
5. services/taskService.js:16 — getById() 호출
   └─ SELECT * FROM tasks WHERE id = ?
   ↓
6. controllers/taskController.js:41 — toTaskDTO(task)
   └─ task 객체 → JSON 응답 포맷
   ↓
7. HTTP 201 응답 (Content-Type: application/json)
```

### 에러 경로 (title 없을 때)

```
validateTaskInput(req.body) → { valid: false }
  → res.status(400).json({ error: "title 은 필수입니다" })
  → HTTP 400 응답 (서비스/DB 도달 안 함)
```

---

## (6) 숨은 규약과 약점 발견

### 발견된 약점 7가지

| 우선순위 | 파일:라인 | 문제점 | 심각도 |
|---------|----------|--------|--------|
| **1** | `services/taskService.js:20` | **status enum 검증 부재** — 임의의 값 저장 가능 | 🔴 |
| **2** | `controllers/taskController.js:35-51` | **create/update 검증 비대칭** — update는 검증 없음 | 🔴 |
| **3** | `controllers/taskController.js:25-28` | **listView에서 DTO 미변환** — 뷰/API 데이터 처리 불일치 | 🟠 |
| **4** | `routes/taskRoutes.js:9` | **PUT 폼 미지원** — HTML form으로 상태 변경 불가 | 🟠 |
| **5** | `controllers/taskController.js:44-51` | **update 검증 부재** — 빈 업데이트도 허용 | 🟡 |
| **6** | `controllers/taskController.js:53-60` | **응답 일관성 부족** — DELETE만 본문 없음 | 🟡 |
| **7** | `services/taskService.js:12-17` | **description/assignee 검증 부재** — 타입 검증 없음 | 🟡 |

### 숨은 규약

- **검증 위치**: Controllers만 검증, Services는 신뢰
- **DTO 변환**: API 응답만 (뷰는 원본 데이터 사용)
- **부분 업데이트**: PUT이 PATCH처럼 동작 (COALESCE 사용)
- **자동 값**: status='todo', created_at=now() (INSERT에서만 적용)

---

## 최종 평가

| 항목 | 결과 |
|------|------|
| 계층 구조 | ✅ 정상 (단방향) |
| 아키텍처 | ✅ 견고함 |
| 입력 검증 | ⚠️ 부분적 (title만) |
| 상태 코드 | ✅ 규약 따름 |
| 데이터 모델 | ⚠️ enum 검증 없음 |
| 약점 | 🔴 7가지 발견 |

